@model webApp.Models.Staff

<div class="m-2 p-3 border border-danger border-3">

    <h2>Scientific Achievements Info.</h2>

    <a asp-controller="ScientificAchievements" asp-action="Create" asp-route-staffId="@Model?.Id" class="btn btn-danger m-4 d-block w-50 m-auto">
        Create New Scientific Achievement
    </a>



    @{
        if (Model.ScientificAchievements != null && Model.ScientificAchievements.Count > 0)
        {
            <table class="table table-striped text-center">
                <thead>
                    <tr>
                        <th>
                            Title
                        </th>
                        <th>
                            Link
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ScientificAchievements)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Title)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Date)
                            </td>
                            <td>
                                <a asp-controller="ScientificAchievements" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                                <a asp-controller="ScientificAchievements" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                                <a asp-controller="ScientificAchievements" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <center>
                <img src="~/images/static_img/No Data Found.jpg" alt="No Data Found" class="w-25 m-3  d-block border border-2 border-danger" />
            </center>
        }
    }
</div>
